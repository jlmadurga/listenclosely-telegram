.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install listenclosely-telegram

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv listenclosely-telegram
    $ pip install listenclosely-telegram
